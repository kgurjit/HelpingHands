	<!-- Title -->
    <div class="row">
        <div class="col-lg-12">
            <h1>Search Results for {{q}}</h1>
        </div>
    </div>
    <!-- /.row -->
    {{#data}}
    	{{name}}<img src="{{pic}}" class="listing-pic"><br>
    {{/data}}